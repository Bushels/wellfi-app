# 2026-05 Journal

## 2026-05-06 — Claude (Opus 4.7)
**Session:** Phase 3 standardization | **Branch:** main | **Commit:** uncommitted

### Intent
Complete the standard MPS portfolio layout for wellfi-app per the design doc at `MPS/docs/plans/2026-04-30-mps-portfolio-restructure.md`. Most of the layout (AGENTS.md, CLAUDE.md, .claude/skills/, .codex/config.toml, .gitignore) already exists from earlier work. This commit adds the missing PROJECT_STATE.md + docs/journal/ structure.

### Changes
- Created `PROJECT_STATE.md` (current truth, deployment URL, backup safety, pending follow-ups).
- Created `docs/journal/README.md` (schema per entry; rationale for journals separate from AGENTS.md/CLAUDE.md).
- Created `docs/journal/2026-05.md` (this file — initial Phase 3 entry).
- Created `.claude/agents/.gitkeep` (empty stub directory; project-specific agents not yet defined; cross-cutting ones live globally at `~/.claude/agents/`).

### Validation
- `git status` shows new files only — no React source touched.
- Production deploy at `21fe2451` unaffected; this commit is docs-only.

### Risks / Open
- Untracked sibling repo dependencies: `Data/` references in some `docs/plans/` archive files now point at the separate `operator-data` repo. Active source code (`src/`) needs a grep audit to confirm no hardcoded `Data/...` paths.
- The vercel.json from Obsidian root was correctly dropped by filter-repo extraction; Vercel auto-detection works at the new repo root. If Vercel build settings drift, vercel.ts (per the new Vercel knowledge update) is the recommended config format.

### Next
- Grep `src/` for `Data/` path literals — fix any production code that still references the old monorepo Data/ path.
- Apply Phase 3 to other 3 portfolio projects (downhole-design ✓, petro-roundtable ✓, operator-data ✓ — wellfi-app is last).
- E3.5 untracked Data/ overlay decisions for operator-data.
- Step 6 — delete MPS/Obsidian/ and decide WellFi/ marketing folder fate.

---

## Pre-Phase-3 history (for context)

### Phase 0: 2026-04-29 to 2026-04-30
- Skill moves into `.claude/skills/`
- Codex config bumped 5.4 → 5.5
- GEMINI.md deleted (Gemini not in active use)
- Doc path references updated

### Phase 0.5: 2026-04-30 to 2026-05-01
- Three.js Wellbore Navigator (3D, multi-lateral, casing-shoe split, reservoir slab)
- WellTrajectory3D.tsx + trajectoryGeometry3D.ts + directionalSurvey.ts
- @react-three/fiber 9.6.0, @react-three/drei 10.7.7, three 0.184.0 (pinned)
- Diagnostic logging: `[WellFi/3D]` prefix
- Vercel deploy errors diagnosed (Root Directory mismatch) → vercel.json override
- VITE_ env vars added to Vercel (Production + Preview)
- mobile-web-app-capable meta tag added

### Phase 2 E4: 2026-05-03 to 2026-05-06
- `git filter-repo --subdirectory-filter wellfi-app/`
- Stripped `wellfi-app/` prefix from 8 doc files
- Build verified locally + Vercel preview before force-push
- `--force-with-lease origin HEAD:main` succeeded after `git fetch origin` refresh
- Production deploy ✓ Ready 51s, identical chunks to local build
